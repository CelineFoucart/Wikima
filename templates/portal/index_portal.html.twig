{% extends 'base.html.twig' %}

{% block title %}Index des portails{% endblock %}

{% block body %}
    <article class="my-5 container-fluid">
        <h2 class="list-title">Les portails</h2>
        <p class="text-center text-muted fs-5 mb-4">Explorer les portails disponibles sur le site</p>
        <p class="mt-5">
            {% set total = portals.getTotalItemCount %}            
            {% set current = portals|length %}
            Affichage de <span class="fw-bold">{{ current }}</span>
            sur <span class="fw-bold">{{ total }}</span>
            portail{{ total > 1 ? 's' : '' }}.
        </p>

        <div class="column-3 mx-2">
            {% for key, elements in items %}
                <div class="mb-4">
                    <h3 class="h4 mb-0 fw-bold">{{ key }}</h3>
                    <ul>
                        {% for item in elements %}
                            <li data-bs-toggle="tooltip" title="{{ item.description ? item.description : 'Aucune description' }}">
                                <a href="{{ path('app_portal_show', {slug: item.slug}) }}" class="text-decoration-none">
                                    {{ item.title }}
                                </a>
                            </li>
                        {% endfor %}
                    </ul>
                </div>
            {% endfor %}
        </div>

        <div class="navigation">
            {{ knp_pagination_render(portals) }}
        </div>
    </article>


{% endblock %}
