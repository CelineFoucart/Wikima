<div class="d-flex flex-wrap p-2" style="gap:7px">
	{% for image in images %}
		{% include "image/components/_thumbnail.html.twig" with {image:image} %}
	{% else %}
		<div class="mt-4 text-muted text-center">Il n'y a aucune image dans cette section.</div>
	{% endfor %}
</div>
