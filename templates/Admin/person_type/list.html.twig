{% extends 'admin_layout.html.twig' %}

{% block title %}Liste des types de personnage{% endblock %}

{% block activePerson %} active show {% endblock %}
{% block activePersonType %}active{% endblock %}

{% block page_title %}Types de personnage{% endblock %}

{% block breadcrumb %}
    <li class="breadcrumb-item active">
        Types de personnage
    </li>
{% endblock %}

{% block toolbar %}
    <a href="{{ path('admin_app_persontype_create') }}" class="btn btn-default btn-sm">
        <i class="fas fa-plus-circle" aria-hidden="true"></i>
        Ajouter
    </a>
{% endblock %}

{% block body %}
    <div class="card border-top-primary">
        <div class="card-body card-show">
            <table class="table data-table mb-0 table-striped">
                <thead>
                    <tr>
                        <th class="table-id">Id</th>
                        <th>Titre</th>
                        <th>Slug</th>
                        <th data-orderable="false">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for personType in personTypes %}
                        <tr>
                            <td>
                                <a href="#" class="fw-bold">{{personType.id }}</a>
                            </td>
                            <td>{{personType.title }}</td>
                            <td>{{personType.slug }}</td>
                            <td>
                                <div class="btn-group">
                                    <a href="{{ path('app_person_type', {slug: personType.slug}) }}" class="btn btn-sm btn-default" title="Consulter">
                                        <i class="fas fa-book-reader"></i>
                                    </a>                    
                                    <a href="{{ path('admin_app_persontype_show', {id: personType.id}) }}" class="btn btn-sm btn-default" title="Afficher">
                                        <i class="fas fa-eye" aria-hidden="true"></i>
                                    </a>
                                    <a href="{{ path('admin_app_persontype_edit', {id: personType.id}) }}" class="btn btn-sm btn-default" title="Éditer">
                                        <i class="fas fa-pencil-alt" aria-hidden="true"></i>
                                    </a>
                                    {% include "Admin/components/_delete_modal.html.twig" with { element: personType, path: 'admin_app_persontype_delete' } %}
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}

