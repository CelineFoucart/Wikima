{{ form_start(form) }}
    <div class="card border-top-primary">
        <div class="card-header">
            <h2 class="card-title h5 mb-0">Note</h2>
        </div>
        <div class="card-body">
            {{ form_widget(form) }}
        </div>
        <div class="card-footer">
            {% include "Admin/components/_form_btns.html.twig" %}
        </div>
    </div>
{{ form_end(form) }}
