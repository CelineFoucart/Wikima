{% extends 'admin_layout.html.twig' %}

{% block title %}Liste des types d'article{% endblock %}

{% block activeWiki %} active show {% endblock %}
{% block activeArticleType %}active{% endblock %}

{% block page_title %}Types d'article{% endblock %}

{% block breadcrumb %}
    <li class="breadcrumb-item active">
        Types d'article
    </li>
{% endblock %}

{% block toolbar %}
    <a href="{{ path('admin_app_articletype_create') }}" class="btn btn-default btn-sm">
        <i class="fas fa-plus-circle" aria-hidden="true"></i>
        Ajouter
    </a>
{% endblock %}

{% block body %}
    <div class="card border-top-primary">
        <div class="card-body p-1">
            <table class="table data-table mb-0 table-striped">
                <thead>
                    <tr>
                        <th class="table-id">Id</th>
                        <th>Titre</th>
                        <th>Slug</th>
                        <th>Icône</th>
                        <th data-orderable="false">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for articleType in articleTypes %}
                        <tr>
                            <td data-label="Id">
                                <a href="#" class="fw-bold">{{articleType.id }}</a>
                            </td>
                            <td data-label="Titre">{{articleType.title }}</td>
                            <td data-label="Slug">{{articleType.slug }}</td>
                            <td data-label="Icône"><i class="{{ articleType.icon }}" data-bs-toggle="tooltip" title="{{ articleType.icon }}"></i></td>
                            <td data-label="Actions">
                                <div class="btn-group">
                                    <a href="{{ path('app_articletype_show', {slug: articleType.slug}) }}" class="btn btn-sm btn-default" data-bs-toggle="tooltip" title="Consulter">
                                        <i class="fas fa-book-reader"></i>
                                    </a>                    
                                    <a href="{{ path('admin_app_articletype_show', {id: articleType.id}) }}" class="btn btn-sm btn-default" data-bs-toggle="tooltip" title="Afficher">
                                        <i class="fas fa-info-circle"></i>
                                    </a>
                                    <a href="{{ path('admin_app_articletype_edit', {id: articleType.id}) }}" class="btn btn-sm btn-default" data-bs-toggle="tooltip" title="Éditer">
                                        <i class="fas fa-pencil-alt" aria-hidden="true"></i>
                                    </a>
                                    {% include "Admin/components/_delete_modal.html.twig" with { element: articleType, path: 'admin_app_articletype_delete' } %}
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}

