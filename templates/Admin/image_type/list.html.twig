{% extends 'admin_layout.html.twig' %}

{% block title %}Liste des types d'image{% endblock %}

{% block activeImage %} active show {% endblock %}
{% block activeImageType %}active{% endblock %}

{% block page_title %}Types d'image{% endblock %}

{% block breadcrumb %}
    <li class="breadcrumb-item active">
        Types d'image
    </li>
{% endblock %}

{% block toolbar %}
    <a href="{{ path('admin_app_imagetype_create') }}" class="btn btn-default btn-sm">
        <i class="fas fa-plus-circle" aria-hidden="true"></i>
        Ajouter
    </a>
{% endblock %}

{% block body %}
    <div class="card border-top-primary">
        <div class="card-body p-1">
            <table class="table data-table mb-0 table-striped">
                <thead>
                    <tr>
                        <th>Titre</th>
                        <th>Slug</th>
                        <th data-orderable="false">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for imageType in imageTypes %}
                        <tr>
                            <td data-label="Titre">{{imageType.title }}</td>
                            <td data-label="Slug">{{imageType.slug }}</td>
                            <td data-label="Actions">
                                <div class="btn-group">
                                    <a href="{{ path('app_image_type', {slug: imageType.slug}) }}" class="btn btn-sm btn-default" data-bs-toggle="tooltip" title="Consulter">
                                        <i class="fas fa-book-reader"></i>
                                    </a>
                                    <a href="{{ path('admin_app_imagetype_show', {id: imageType.id}) }}" class="btn btn-sm btn-default" data-bs-toggle="tooltip" title="Afficher">
                                        <i class="fas fa-info-circle"></i>
                                    </a>
                                    <a href="{{ path('admin_app_imagetype_edit', {id: imageType.id}) }}" class="btn btn-sm btn-default" data-bs-toggle="tooltip" title="Éditer">
                                        <i class="fas fa-pencil-alt" aria-hidden="true"></i>
                                    </a>
                                    {% include "Admin/components/_delete_modal.html.twig" with { element: imageType, path: 'admin_app_imagetype_delete' } %}
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}

